# YT 家具官网 - API 服务依赖清单（集中管理于 dependency/api/）
# 安装方式：从本地 wheels 离线安装（见 dependency/api/wheels/）
#   python -m pip install --no-index --find-links dependency/api/wheels -r dependency/api/requirements.txt

# --- Web 框架 ---
fastapi==0.115.*
uvicorn[standard]==0.30.*

# --- ORM / 数据库 ---
sqlalchemy==2.0.*
# 开发期使用 SQLite，无需额外驱动；如需 MySQL/Postgres 再补充

# --- 数据校验 / 配置 ---
pydantic==2.*
pydantic-settings==2.*
email-validator==2.*

# --- 安全 / 认证 ---
PyJWT==2.*
passlib[bcrypt]==1.7.*
python-multipart==0.0.*

# --- 测试 ---
pytest==8.*
httpx==0.27.*
