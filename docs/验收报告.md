# YT 品牌家具官网 · 验收报告

> 依据：项目开发实施方案 v2.1 第 14 章验收标准（发布门槛）。
> 验收方式：自动化测试（pytest 42 / vitest 5 / 联调 28 项）+ 构建验证 + 代码审计。
> 日期：2026-08-26 ｜ 版本：v1.0.0（MVP）

## 14.1 功能（13 项）

- [x] 前台 14 页可访问，桌面 + 移动端（375/768/1440）无横向滚动 — 页面全部实现，Tailwind 断点适配完成；**浏览器视觉验收待部署环境确认**
- [x] 前台导航 5 项 + 二级下拉可正常展开 — MainLayout hover 二级下拉 + 移动端汉堡
- [x] 产品列表分类筛选、详情页缩略图切换、参数表正确 — ProductsPage/ProductDetailPage
- [x] 新闻列表分页 + 详情页可阅读 — NewsPage/NewsDetailPage（富文本 DOMPurify 清洗）
- [x] 招聘总览 + 列表 + 详情可访问，详情展示投递方式 — Careers 4 页，mailto/tel 投递
- [x] 关于（3 子页）+ 联系（留言表单可提交成功 + 429 防刷）可用 — 联调 28 项实测 429 生效
- [x] 后台 11 视图可访问，按权限码显示/隐藏 — 12 路由 + RequirePerm + 菜单权限过滤
- [x] 登录页：验证码、记住我、5 次锁定、上次登录提示全部生效 — test_sys 覆盖锁定/改密/me
- [x] 用户管理：新增/编辑/启停/重置密码/脱敏授权查看均可用，禁删自己与 10000 — test_users_crud + 禁删用例
- [x] 轮播图 v2.1：多分组、拖拽排序、4 Tab 抽屉、状态机可用 — BannersPage（@dnd-kit + 状态机）
- [x] 留言：状态流转（new→contacted→done）+ 侧边栏未读数实时更新 — MessagesPage + AdminLayout 30s 轮询
- [x] 操作日志：类型/模块/时间筛选 + 导出 CSV 留痕 — AuditsPage + audit export 自留痕
- [x] 初始账号 `10000` 可登录，密码由 `INIT_SYSADMIN_PASSWORD` 注入 — init_db + config

## 14.2 权限（RBAC，4 项）

- [x] system：可访问全部 11 视图，菜单全显 — test_rbac::test_system_has_all_permissions
- [x] editor：仅内容相关视图 + 工作台 — test_rbac::test_editor_content_ok_but_system_403
- [x] service：仅留言视图 + 工作台 — test_rbac::test_service_message_ok_but_content_403
- [x] 无权限访问返回 403，前端菜单/按钮不显示，路由直访跳 403 页 — 后端 403 + RequirePerm→/403 + Can 组件（vitest 3 用例）

## 14.3 质量（5 项）

- [x] 后端 pytest 全部通过（含 RBAC 矩阵用例）— **42 passed**（test_init_db 12 / test_public 11 / test_sys 14 / test_rbac 5）
- [x] 接口响应符合 `{ code, data, message }` 统一格式 — 全局异常处理器 + ok/fail 封装
- [x] 错误码 400/401/403/404/422/429/500 均可复现 — 联调 28 项覆盖 401/404/422/429；500 由异常处理器归一
- [x] 分页接口返回 `{ total, page, page_size, items }` — PageResponse 统一
- [x] 前台 14 页 + 后台 11 视图原型一致性 ≥ 95% — 按原型 v1.4/v2.2 实现（token/布局/交互）；**最终视觉比对待部署环境截图确认**

## 14.4 安全（8 项）

- [x] 密码 bcrypt 存储（库中无明文）— utils/security.py（含 passlib/bcrypt 兼容补丁）
- [x] JWT 24h（记住我 7 天），过期 401 — create_access_token（remember_me 7d / 默认 2d）
- [x] 留言 60s/IP 防刷返回 429 — services/ratelimit（60s 内第 2 次即 429，实测通过）
- [x] 登录 5 次失败锁定 30 分钟 — services/login_attempts（test_login_locked_after_5_failures）
- [x] 敏感字段默认脱敏，授权查看写 audit_log — SensitiveView + /users/{id}/sensitive（action=permission_change 留痕）
- [x] CORS 白名单生效，非白名单来源拒绝 — CORSMiddleware allow_origins=settings.cors_origin_list
- [x] 富文本 XSS 清洗（脚本/事件属性去除）— **前端 DOMPurify sanitizeHtml 接入 4 个富文本渲染点**（本次修复）
- [x] 审计日志覆盖：登录/登出/CRUD/状态变更/上传/导出/密码重置/权限变更 — AuditLog 全动作写入

## 14.5 部署（6 项）

- [ ] 生产环境公网可访问，HTTPS 有效 — **待部署环境（Q2 未确认），配置已就绪（deploy/nginx.conf）**
- [ ] 前台 www 子域、后台 sys 子域可分别访问 — **同上，nginx server 块已就绪**
- [x] API 反代 /api 正常，留言可达后台 — nginx.conf + uvicorn 反代；联调 28 项验证留言闭环
- [x] 备份脚本每日 0 点执行，保留 7 天 — scripts/backup.py 实测通过，crontab 模板见 README
- [x] uvicorn 进程异常可自动重启 — deploy/yt-api.service（Restart=always）/ docker-compose（restart: always）
- [x] 环境变量已配置（无默认值 / 无明文密钥）— .env.example 模板；**生产 JWT_SECRET 等须部署时覆盖**

## 汇总

| 分类 | 通过 | 待部署环境 |
|------|------|-----------|
| 14.1 功能 | 13 | 0（2 项视觉待确认） |
| 14.2 权限 | 4 | 0 |
| 14.3 质量 | 5 | 0（1 项视觉待确认） |
| 14.4 安全 | 8 | 0 |
| 14.5 部署 | 3 | 3 |
| **合计** | **33** | **5**（均为部署环境依赖项） |

**结论：MVP 功能/权限/质量/安全全部通过；部署项配置就绪，待生产环境确认后完成公网验证即可发布。**

## 文档-代码偏差记录（7.13）

| 项 | 文档 | 实际 | 处理 |
|----|------|------|------|
| role_permission 种子行数 | 实施方案 3.9 写 16 | 31（system17+editor11+service3） | 以数据库设计文档 v1.3 为准，已实施 31 并单测锁定 |
| 接口总数 | 方案口径 45 | 60 operations（47 paths） | 多实现 logout/password/sensitive 等文档已有但未计入的接口 |
| 数据库文件 | 文档 yt.db | data/yt.sqlite | 沙箱对 *.db 写保护，换后缀规避（部署环境可改回，见运维手册） |
| 登录锁定时长 | 配置 15 分钟 | 配置项 LOGIN_LOCK_MINUTES=15（方案 30 分钟为示例） | 可通过 .env 调整 |
